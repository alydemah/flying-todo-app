import React from 'react';
import PropTypes from "prop-types";

const TodoList = props =>
    <ul>


        <Todo
            key={index}
            name={todo.title}
            
        />

     </ul>;
     
     

     
     export default TodoList;
